name: sync-bureau-daily

on:
  schedule:
    - cron: '0 9,15,21 * * *'  # 06h, 12h, 18h horário de Brasília (UTC-3)
  workflow_dispatch:             # permite rodar manualmente pelo GitHub UI

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 3

    steps:
      - name: Invocar Edge Function
        run: |
          RESPONSE=$(curl -s -w "\n%{http_code}" -X POST \
            "https://ftmgmfdqdqxboiktxcoj.supabase.co/functions/v1/sync-bureau-daily" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            -H "Content-Type: application/json" \
            -d '{}')

          BODY=$(echo "$RESPONSE" | head -n -1)
          STATUS=$(echo "$RESPONSE" | tail -n 1)

          echo "HTTP $STATUS"
          echo "$BODY"

          if [ "$STATUS" != "200" ]; then
            echo "::error::Sync falhou com HTTP $STATUS"
            exit 1
          fi
